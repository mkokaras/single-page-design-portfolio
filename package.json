{
  "name": "single-page-design-portfolio-code",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "sass": "^1.64.1",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
